import 'package:flutter/material.dart';

class TrackingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Track Order')),
      body: Center(child: Text('Track your order here')),
    );
  }
}
